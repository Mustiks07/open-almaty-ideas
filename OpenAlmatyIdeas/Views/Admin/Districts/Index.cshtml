@model IEnumerable<OpenAlmatyIdeas.Domain.Entities.District>

@{
    ViewData["Title"] = L["Admin.Districts"].Value;
    Layout = "_AdminLayout";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>@L["Admin.Districts"]</h1>
    <a href="/admin/districts/edit" class="btn btn-success">@L["Admin.AddDistrict"]</a>
</div>

<table class="table table-hover">
    <thead>
        <tr>
            <th>@L["Admin.ColId"]</th>
            <th>@L["Admin.ColName"]</th>
            <th>@L["Admin.ColCount"]</th>
            <th>@L["Admin.ColActions"]</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var district in Model)
        {
            <tr>
                <td>@district.Id</td>
                <td>@district.Title</td>
                <td>@(district.Proposals?.Count ?? 0)</td>
                <td>
                    <a href="/admin/districts/edit/@district.Id" class="btn btn-sm btn-outline-primary">
                        @L["Admin.Edit"]
                    </a>
                    <form action="/admin/districts/delete/@district.Id" method="post" class="d-inline"
                          onsubmit="return confirm('@L["Admin.DeleteDistrictConfirm"]')">
                        <button type="submit" class="btn btn-sm btn-outline-danger">@L["Admin.Delete"]</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
